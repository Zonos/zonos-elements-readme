/**
 * Since the backend and the frontend are using the same keys,
 * make sure to not change the key value below since if it changes
 * and the frontend script is cached, the formula may not work as expected.
 */
export declare const DUTY_KEY: string;
export declare const NON_ZONOS_FEE_KEY: string;
export declare const TAXES_KEY: string;
export declare const ZONOS_FEE_KEY: string;
