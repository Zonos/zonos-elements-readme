/**
 * Checks if the short address code input should be rendered based on the country code.
 */
export declare const shouldRenderShortAddressCodeInput: ({ countryCode, }: {
    countryCode: string;
}) => boolean;
