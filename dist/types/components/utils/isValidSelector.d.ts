/**
 * Check if selector user pass is valid
 * @example
 * isValidSelector('div') // true
 * isValidSelector('div + >>> span') // false
 */
export declare const isValidSelector: (selector: string) => boolean;
