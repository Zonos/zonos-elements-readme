/**
 * Get the Zonos QA URL and return it. Will remove `zonosQaUrl` from the url query if it doesn't exists.
 */
export declare const getZonosQaUrl: () => string | null;
/**
 * Get the Zonos QA URL and return it. Will remove `zonosQaUrl` from the url query if it doesn't exists.
 */
export declare const getZonosQaUrlApi: () => string | null;
